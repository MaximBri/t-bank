# Матрица трассировки требований

## Назначение

Матрица показывает соответствие требований из ТЗ:

- тест-кейсам
- smoke-checklist
- автотестам по уровням пирамиды тестирования
- зафиксированным дефектам

## Пирамида тестирования

- `Unit` — модульные проверки frontend/backend логики
- `API` — контрактные и baseline API tests
- `E2E` — пользовательские сквозные сценарии и smoke UI
- `Manual` — test cases / checklist / exploratory

## Статусы

- `Covered` — требование покрыто артефактами и/или автотестами
- `Partial` — покрыто частично
- `Planned` — покрытие запланировано, но не завершено
- `Blocked by bug` — покрытие упирается в открытые дефекты

## Матрица

| Req ID | Требование из ТЗ | Test Cases | Smoke | Unit | API | E2E | Баги | Status | Комментарий |
|---|---|---|---|---|---|---|---|---|---|
| REQ-001 | Регистрация и авторизация пользователей | `TC-001`, `TC-002`, `TC-UI-01`, `TC-UI-02` | `SMK-01`, `SMK-08`, `SMK-20`, `SMK-21` | Не трассировано отдельно | `auth` domain covered, positive + negative | UI smoke для login/register, auth e2e есть | `BUG-004`, `BUG-005`, `BUG-006`, `BUG-007`, `BUG-008` | Covered | Базовое API и E2E покрытие есть |
| REQ-002 | Создание события / поездки: название, даты, описание | `TC-003`, `TC-UI-05` | `SMK-02`, `SMK-23` | Не трассировано отдельно | `events` domain: create/list/get/update baseline | UI smoke для create event screen, home | `BUG-013` | Partial | `POST /events` покрыт, `PATCH /events/{eventId}` сейчас падает `500` |
| REQ-003 | Роли: владелец и участник | `TC-005`, `TC-006` | `SMK-04`, `SMK-05` | Не трассировано отдельно | `participants` domain covered частично | Частично через event/profile smoke | — | Partial | По текущему контракту покрыт только `GET /events/{eventId}/participants` |
| REQ-004 | Приглашение участников по ссылке и подтверждение участия | `TC-004`, `TC-005`, `TC-UI-04` | `SMK-03`, `SMK-04` | Не трассировано отдельно | По текущему `openapi.raw` домен не представлен | UI smoke для invite modal пока не завершён | — | Planned | Требование есть в ТЗ, но текущий контракт неполный |
| REQ-005 | Категории бюджета: дефолтные и пользовательские | — | — | Не трассировано отдельно | В текущем `openapi.raw` endpoints отсутствуют | Нет | — | Planned | В старом контракте были categories, в текущем отсутствуют |
| REQ-006 | Добавление расходов: плательщик, участники, категория, сумма, комментарий | `TC-007`, `TC-UI-08`, `TC-UI-09`, `TC-UI-10`, `TC-UI-11` | `SMK-06` | Не трассировано отдельно | `expenses` domain: list/create baseline | UI expense pages/forms ещё не добиты | `BUG-012` | Partial | `POST /events/{eventId}/expenses` в baseline есть, positive сейчас выявляет `500` |
| REQ-007 | Автоматический расчёт долей при равном делении расхода | `TC-008` | `SMK-07` | Не трассировано отдельно | Нет отдельного контрактного покрытия | Нет | — | Planned | Отдельного settlement/ratio контракта в текущем `openapi.raw` нет |
| REQ-008 | Пересчёт сальдо после добавления или изменения расходов | `TC-008` | `SMK-07` | Не трассировано отдельно | Нет отдельного контрактного покрытия | Нет | — | Planned | Требование зависит от сценариев расходов/итогов |
| REQ-009 | История событий и действий участников | — | `SMK-10` | Не трассировано отдельно | В текущем `openapi.raw` endpoint истории отсутствует | Нет | — | Planned | В ТЗ требование есть, в текущем контракте не представлено |
| REQ-010 | Экспорт итогов в CSV | — | — | Не трассировано отдельно | Endpoint в текущем контракте отсутствует | Нет | — | Planned | Требование есть в ТЗ, но контракт/реализация не трассированы |
| REQ-011 | Личный кабинет / профиль пользователя | `TC-012`, `TC-013`, `TC-UI-16` | `SMK-17`, `SMK-18`, `SMK-25` | Не трассировано отдельно | `profile` domain: `GET /me`, `PATCH /me`, `POST /me/password` baseline | UI smoke для profile page есть | — | Covered | Базовое API покрытие и UI smoke есть |

## Покрытие по артефактам

### API baseline

Актуальные домены текущего `openapi` контракта, заведенные в автотестах:

- `auth`
- `events`
- `expenses`
- `profile`
- `participants`

Доменов нет в текущем контракте / неактуальны для базовой проверки:

- `categories`
- `invite-links`
- `settlements`

### E2E Smoke (UI)

Сейчас заведены:

- `SMK-20` / `TC-UI-01` — login page
- `SMK-21` / `TC-UI-02` — register page
- `SMK-22` / `TC-UI-03` — home page
- `SMK-23` / `TC-UI-05` — create event screen
- `SMK-25` / `TC-UI-16` — profile page

Не закончены:

- `SMK-24` / `TC-UI-06` — event page
- последующие expense / invite / leave event UI сценарии

## Комментарий

Матрица отражает текущее состояние проекта:

- часть требований уже закрыта тестированием API и E2E smoke-тестами
- часть требований присутствует в ТЗ, но отсутствует в текущем `openapi` контракте
- часть positive API сценариев уже выявляет реальные backend-дефекты `500`
